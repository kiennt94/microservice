spring:
  application:
    name: account-service
  profiles:
    active: dev
  config:
    import: optional:configserver
  cloud:
    config:
      uri: http://192.168.1.99:8888
      username: admin
      password: admin
    discovery:
      enabled: true
